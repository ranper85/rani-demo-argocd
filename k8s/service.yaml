apiVersion: v1
kind: Service                                                             
metadata:                                                                 
  name: rani-argocd-demo
  labels:
    app: rani-argocd-demo
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: rani-argocd-demo
